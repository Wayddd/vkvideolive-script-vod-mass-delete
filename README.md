# VK Video Live Cleaner 🗑️

**JS-скрипт для массового удаления записей стримов с платформы VK Play Live / VK Video.**

Если вы стримите на VK Play Live, то знаете, что платформа автоматически сохраняет записи всех трансляций. Удалять их вручную через меню «три точки» -> «Удалить» -> «Подтвердить» очень долго, особенно если записей накопилось больше сотни. Кнопки "Удалить всё" на сайте нет.

Этот скрипт делает это за вас, эмулируя действия пользователя.

## 🚀 Как это работает
Скрипт запускается в консоли браузера. Он:
1. Находит кнопку меню на видео.
2. Имитирует реальный клик мыши (обходит защиту, где обычный `.click()` не работает).
3. Нажимает «Удалить».
4. Ждет появления окна подтверждения и нажимает красную кнопку «Удалить».
5. Повторяет процесс для следующего видео.

## ⚙️ Инструкция по запуску

1. Зайдите на [live.vkvideo.ru](https://live.vkvideo.ru) (или vkplay.live), авторизуйтесь и перейдите на **Свой канал** -> вкладка **Видео** (или Трансляции).
2. Откройте консоль разработчика в браузере:
   * Нажмите клавишу `F12`.
   * Или кликните правой кнопкой мыши в любом месте страницы -> **Просмотреть код** -> вкладка **Console**.
3. Скопируйте код ниже целиком.
4. Вставьте его в консоль и нажмите `Enter`.
5. Не закрывайте вкладку, пока скрипт работает.

## 💻 Скрипт (Копировать отсюда)

```javascript
async function deleteStreamsV3() {
    const delay = (ms) => new Promise(res => setTimeout(res, ms));

    // Функция для жесткой эмуляции клика (обходит ошибку "not a function")
    function simulateClick(element) {
        const event = new MouseEvent('click', {
            view: window,
            bubbles: true,
            cancelable: true
        });
        element.dispatchEvent(event);
    }

    // Актуальные селекторы классов VK (на 2025 год)
    const DOTS_CONTAINER = '.RecordExtraActions_dotsContainer_nt0vo';
    const DELETE_MENU_ITEM = '.RecordExtraActions_menuItemDelete_Zhvb8';
    const CONFIRM_BTN = '.PopupFormik_button_NbwZb';

    console.log("🚀 Запуск скрипта VK Cleaner...");
    let count = 0;
    let errors = 0;

    while (true) {
        // 1. Ищем все контейнеры с кнопками меню
        const allDots = document.querySelectorAll(DOTS_CONTAINER);

        if (allDots.length === 0) {
            console.log("✅ Видимые видео удалены. Если остались старые записи — прокрутите страницу вниз и запустите скрипт снова.");
            break;
        }

        // Берем первое видео в списке
        const currentContainer = allDots[0];
        
        // Скроллим к нему, чтобы элемент был в зоне видимости
        currentContainer.scrollIntoView({ block: 'center', behavior: 'instant' });
        await delay(300);

        try {
            // 2. Кликаем по иконке меню (ищем span или svg внутри контейнера)
            const targetClick = currentContainer.querySelector('span') || currentContainer.querySelector('svg') || currentContainer;

            console.log(`🔹 Обработка видео #${count + 1}...`);
            simulateClick(targetClick); 
            
            await delay(800); // Ждем открытия меню

            // 3. Ищем кнопку "Удалить" в выпадающем списке
            const deleteBtn = document.querySelector(DELETE_MENU_ITEM);
            
            if (!deleteBtn) {
                console.warn("⚠️ Меню не открылось или кнопка не найдена. Пробую повторить...");
                document.body.click(); // Клик в пустоту для сброса
                await delay(500);
                errors++;
                // Если застряли на одном видео — удаляем его из DOM локально и идем дальше
                if (errors > 3) {
                    console.log("Пропуск проблемного элемента...");
                    currentContainer.remove(); 
                    errors = 0;
                }
                continue;
            }

            simulateClick(deleteBtn);
            await delay(800); // Ждем модальное окно подтверждения

            // 4. Ищем кнопку подтверждения (по тексту "Удалить")
            const allPopupBtns = Array.from(document.querySelectorAll(CONFIRM_BTN));
            const confirmBtn = allPopupBtns.find(btn => btn.innerText.trim() === 'Удалить');

            if (confirmBtn) {
                simulateClick(confirmBtn);
                count++;
                console.log(`🗑 Видео удалено. Всего: ${count}`);
                errors = 0; 
                
                // Ждем пока видео исчезнет из списка (важно для синхронизации)
                await delay(2000);
            } else {
                console.error("⛔️ Кнопка подтверждения не найдена. Отмена.");
                document.body.click();
                await delay(500);
            }

        } catch (e) {
            console.error("Ошибка:", e);
            await delay(1000);
        }
    }
    console.log(`🏁 Готово! Удалено записей: ${count}`);
}

deleteStreamsV3();

```

⚠️ Disclaimer (Отказ от ответственности)
Этот скрипт предоставляется "как есть". Автор не несет ответственности за случайно удаленный контент. Удаленные видео невозможно восстановить. Инструмент не является официальной разработкой VK. Используйте на свой страх и риск.
